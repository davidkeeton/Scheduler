# Set this to the home server's LAN address to reach the app from other devices.
# Keep the alpha app off the public internet while automatic admin is enabled.
WEB_BIND=0.0.0.0
DB_PASSWORD=replace-with-a-local-secret
DJANGO_SECRET_KEY=replace-with-another-local-secret
